<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Superhéroe</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-4">
        <h1 class="mb-4">Editar Superhéroe</h1>
        
        <form action="/heroes/<%= superheroe._id %>/editar" method="POST">
            <input type="hidden" name="_method" value="PUT">
            
            <div class="mb-3">
                <label for="nombreSuperheroe" class="form-label">Nombre de Superhéroe</label>
                <input type="text" class="form-control" id="nombreSuperheroe" name="nombreSuperheroe" 
                       value="<%= superheroe.nombreSuperheroe %>" required>
            </div>
            
            <div class="mb-3">
                <label for="nombreReal" class="form-label">Nombre Real</label>
                <input type="text" class="form-control" id="nombreReal" name="nombreReal" 
                       value="<%= superheroe.nombreReal %>" required>
            </div>
            
            <div class="mb-3">
                <label for="edad" class="form-label">Edad</label>
                <input type="number" class="form-control" id="edad" name="edad" 
                       value="<%= superheroe.edad %>" min="0" required>
            </div>
            
            <div class="mb-3">
                <label for="planetaOrigen" class="form-label">Planeta de Origen</label>
                <input type="text" class="form-control" id="planetaOrigen" name="planetaOrigen" 
                       value="<%= superheroe.planetaOrigen || '' %>">
            </div>
            
            <div class="mb-3">
                <label for="poderes" class="form-label">Poderes</label>
                <input type="text" class="form-control" id="poderes" name="poderes" 
                       value="<%= superheroe.poderes.join(', ') %>" required>
                <small class="text-muted">Separados por comas</small>
            </div>
            
            <div class="mb-3">
                <label for="debilidad" class="form-label">Debilidades</label>
                <input type="text" class="form-control" id="debilidad" name="debilidad" 
                       value="<%= superheroe.debilidad ? superheroe.debilidad.join(', ') : '' %>">
                <small class="text-muted">Separadas por comas</small>
            </div>
            
            <button type="submit" class="btn btn-primary">Guardar Cambios</button>
            <a href="/heroes" class="btn btn-secondary">Cancelar</a>
        </form>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>